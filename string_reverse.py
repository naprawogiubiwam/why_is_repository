def string_reverse(text, reverse_text):
    for i in text:
        index = i
        if index == [-1]:
            reverse_text.append(index)
        else:
            continue

        
index = ' '
reverse_text = []
text = input().split()
print(string_reverse(text, reverse_text))
